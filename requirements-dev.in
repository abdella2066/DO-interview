# Test and lint tooling on top of the runtime dependencies:
#   uv pip compile requirements-dev.in --universal -o requirements-dev.txt
-r requirements.in
pytest>=8
pytest-asyncio>=0.23
pytest-cov>=5
httpx>=0.27
ruff>=0.6
